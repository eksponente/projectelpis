<!DOCTYPE html>
<html lang="en">
<body>
{% for item in items %}
    {{ item.id }} <br/>
    {{ item.title }} <br/>
    {{ item.description }} <br/>
    {% for link in item.links %}
        {{ link.url }} <br/>
    {% endfor %}
    <br/>
    <br/>
{% endfor %}
</body>
</html>